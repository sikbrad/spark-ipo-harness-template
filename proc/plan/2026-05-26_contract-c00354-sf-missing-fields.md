# C00354 SF 누락 필드 조사 계획

## 목적
- 포털 PROD `C00354` 계약에서 Salesforce 유래라면 채워졌어야 할 값이 비어 있는지 확인한다.
- 라이브 포털 계약값, 로더 매핑, 원천/이관 데이터 근거를 대조해 리서치 보고서로 남긴다.

## 진행
- [x] PROD 계약 `C00354` 현재값 확인
- [x] SF 로더/매핑 코드에서 계약 필드 채움 조건 확인
- [x] 로컬 원천/이관 데이터에서 `C00354` 관련 행 확인
- [x] 비어 있는 필드와 원인 가설 정리
- [x] `proc/research`에 리서치 보고서 작성

## 주의
- PROD API 우회 수정은 하지 않는다. 사용자가 지정한 Playwright MCP 브라우저로 화면에서 확인/적용한다.
- 공지/Teams 발송은 이번 요청 범위가 아니므로 진행하지 않는다.

## 결과
- Playwright MCP 브라우저로 `admin-test--@doflab.com` 로그인 후 `https://portal.doflab.com/contracts/C00354` 확인.
- 저장 전 `출처: SF`, `계약일` 빈 값, `비고`의 SF 활성화일 `2022-07-15` 확인.
- 사용자 지시에 따라 브라우저에서 계약일 `2022-07-15`를 적용했고 `계약이 수정되었습니다.` 토스트를 확인.
- 저장 후 계약일이 `2022-07-15`로 표시되고 해당 필드가 다시 비활성화되어 최초 1회 입력 예외가 소진된 것을 확인.
- 보고서: `proc/research/2026-05-26_c00354_sf_missing_contract_date.md`
